# ══════════════════════════════════════════════════════════
# RadioZec — MediaMTX Configuration
# Handles OBS RTMP ingest and WebRTC/HLS output for live events
# ══════════════════════════════════════════════════════════

# ── Logging ──
logLevel: info

# ── API (used by Node.js to check stream status) ──
api: yes
apiAddress: 127.0.0.1:9997

# ── RTMP (OBS ingest) ──
rtmp: yes
rtmpAddress: :1935

# ── HLS (playback) ──
hls: yes
hlsAddress: :8888
hlsAllowOrigin: '*'
hlsVariant: fmp4
hlsSegmentCount: 5
hlsSegmentDuration: 1s
hlsPartDuration: 250ms
hlsAlwaysRemux: yes

# ── WebRTC (low-latency playback) ──
webrtc: yes
webrtcAddress: :8889
webrtcAllowOrigin: '*'

# ── Disable unused protocols ──
rtsp: no
srt: no
record: no

# ── Paths ──
paths:
  zecradio-live:
    source: publisher
